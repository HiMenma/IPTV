# Implementation Plan

- [x] 1. Initialize Flutter project and setup dependencies
  - Create new Flutter project with multi-platform support
  - Configure pubspec.yaml with all required dependencies (provider, dio, shared_preferences, file_picker, path_provider, uuid, player_widget)
  - Setup project structure with folders for models, services, repositories, viewmodels, views, and utils
  - Configure platform-specific permissions (Android manifest, iOS Info.plist)
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 1.6, 9.1_

- [x] 2. Implement core data models
  - [x] 2.1 Create Configuration model with JSON serialization
    - Implement Configuration class with all fields (id, name, type, credentials, timestamps)
    - Add toJson() and fromJson() methods
    - Add ConfigType enum (xtream, m3uLocal, m3uNetwork)
    - _Requirements: 4.1_
  - [x] 2.2 Create Channel model with JSON serialization
    - Implement Channel class with fields (id, name, streamUrl, logoUrl, category, configId)
    - Add toJson() and fromJson() methods
    - _Requirements: 2.1, 3.1_
  - [x] 2.3 Create Favorite model with JSON serialization
    - Implement Favorite class with fields (channelId, addedAt)
    - Add toJson() and fromJson() methods
    - _Requirements: 6.1_
  - [x] 2.4 Create BrowseHistory model with JSON serialization
    - Implement BrowseHistory class with fields (channelId, watchedAt)
    - Add toJson() and fromJson() methods
    - _Requirements: 7.1_
  - [x] 2.5 Write property test for data persistence round-trip
    - **Property 12: Data persistence round-trip preserves content**
    - **Validates: Requirements 4.4, 6.3, 7.2**

- [x] 3. Implement repository layer for data persistence
  - [x] 3.1 Create ConfigurationRepository
    - Implement getAll(), getById(), save(), update(), delete() methods
    - Use shared_preferences for storage
    - Handle JSON serialization/deserialization
    - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_
  - [x] 3.2 Write property tests for configuration management
    - **Property 9: Configuration creation persists data**
    - **Property 10: Configuration rename preserves all other fields**
    - **Property 11: Configuration deletion removes from storage**
    - **Validates: Requirements 4.1, 4.2, 4.3**
  - [x] 3.3 Create FavoriteRepository
    - Implement getAll(), isFavorite(), add(), remove() methods
    - Use shared_preferences for storage
    - _Requirements: 6.1, 6.2, 6.3, 6.4_
  - [x] 3.4 Write property tests for favorites management
    - **Property 18: Marking channel as favorite adds to favorites list**
    - **Property 19: Unmarking favorite removes from favorites list**
    - **Validates: Requirements 6.1, 6.2**
  - [x] 3.5 Create HistoryRepository
    - Implement getAll(), add(), clear() methods
    - Use shared_preferences for storage
    - _Requirements: 7.1, 7.2, 7.3, 7.5_
  - [x] 3.6 Write property tests for history management
    - **Property 21: Playing channel records history with timestamp**
    - **Property 22: Browse history displays in reverse chronological order**
    - **Property 23: Clearing history removes all entries**
    - **Validates: Requirements 7.1, 7.4, 7.5**

- [x] 4. Implement M3U parsing service
  - [x] 4.1 Create M3UService with parsing logic
    - Implement parseLocalFile() to read and parse local M3U/M3U8 files
    - Implement parseNetworkFile() to fetch and parse remote M3U/M3U8 files
    - Implement exportToM3U() to convert channel list to M3U format
    - Handle EXTINF metadata extraction (name, logo, category)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 2.5_
  - [x] 4.2 Write property test for M3U parsing
    - **Property 6: M3U/M3U8 parsing extracts all channels**
    - **Validates: Requirements 3.1, 3.2, 3.3, 3.4**
  - [x] 4.3 Write property test for M3U export round-trip
    - **Property 5: Xtream to M3U export round-trip preserves channels**
    - **Validates: Requirements 2.5**
  - [x] 4.4 Write property test for invalid M3U handling
    - **Property 7: Invalid M3U content produces error**
    - **Validates: Requirements 3.5**

- [x] 5. Implement Xtream Codes API service
  - [x] 5.1 Create XtreamService with API integration
    - Implement authenticate() to validate credentials and fetch initial data
    - Implement getChannels() to retrieve channel list
    - Implement getCategories() to retrieve category list
    - Use dio for HTTP requests
    - Handle Xtream API response format
    - _Requirements: 2.1, 2.2, 2.4_
  - [x] 5.2 Write property test for Xtream authentication
    - **Property 1: Xtream authentication with valid credentials succeeds**
    - **Property 3: Invalid Xtream credentials are rejected**
    - **Validates: Requirements 2.1, 2.3**
  - [x] 5.3 Write property test for Xtream configuration persistence
    - **Property 2: Successful Xtream authentication persists configuration**
    - **Validates: Requirements 2.2**
  - [x] 5.4 Write property test for Xtream refresh
    - **Property 4: Xtream configuration refresh updates channel list**
    - **Validates: Requirements 2.4**

- [x] 6. Implement player service with Aliyun Player Widget
  - [x] 6.1 Create PlayerService wrapper
    - Implement initialize(), play(), pause(), resume(), stop() methods
    - Implement setVolume() and dispose() methods
    - Create stateStream and errorStream for player events
    - Integrate Aliyun Player Widget APIs
    - Handle player lifecycle and resource management
    - _Requirements: 5.1, 5.2, 5.4, 5.5, 9.2, 9.4_
  - [x] 6.2 Write property test for player initialization
    - **Property 24: Aliyun Player Widget initializes correctly**
    - **Validates: Requirements 9.2**
  - [x] 6.3 Write property test for playback error handling
    - **Property 16: Invalid stream URLs are handled gracefully**
    - **Property 25: Playback errors are handled appropriately**
    - **Validates: Requirements 5.3, 9.3**
  - [x] 6.4 Write property test for resource cleanup
    - **Property 17: Stopping playback releases resources**
    - **Validates: Requirements 5.4, 9.4**

- [x] 7. Implement ViewModels with state management
  - [x] 7.1 Create ConfigurationViewModel
    - Implement loadConfigurations(), createConfiguration(), renameConfiguration(), deleteConfiguration() methods
    - Implement refreshConfiguration() and exportToM3U() methods
    - Use ChangeNotifier for state management
    - Integrate ConfigurationRepository, XtreamService, and M3UService
    - _Requirements: 4.1, 4.2, 4.3, 4.5, 2.4, 2.5, 3.6_
  - [x] 7.2 Write property test for configuration refresh operations
    - **Property 4: Xtream configuration refresh updates channel list**
    - **Property 8: M3U network configuration refresh fetches latest content**
    - **Validates: Requirements 2.4, 3.6**
  - [x] 7.3 Create ChannelViewModel
    - Implement loadChannels(), loadFavorites(), loadHistory() methods
    - Implement toggleFavorite() and clearHistory() methods
    - Use ChangeNotifier for state management
    - Integrate FavoriteRepository and HistoryRepository
    - _Requirements: 6.1, 6.2, 6.4, 6.5, 7.3, 7.4, 7.5_
  - [x] 7.4 Write property test for application startup data loading
    - **Property 13: Application startup loads all saved data**
    - **Validates: Requirements 4.5, 6.4, 7.3**
  - [x] 7.5 Create PlayerViewModel
    - Implement playChannel(), pause(), resume(), stop() methods
    - Implement setVolume() and toggleFullscreen() methods
    - Use ChangeNotifier for state management
    - Integrate PlayerService and HistoryRepository
    - Record history when channel is played
    - _Requirements: 5.1, 5.2, 5.4, 5.5, 7.1_
  - [x] 7.6 Write property test for playback state management
    - **Property 14: Channel selection initiates playback**
    - **Property 15: Playback displays controls**
    - **Validates: Requirements 5.1, 5.2, 5.5**

- [x] 8. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 9. Build UI screens and navigation
  - [x] 9.1 Create HomeScreen with configuration list
    - Display list of configurations
    - Add buttons for creating new configuration
    - Navigate to configuration detail or channel list
    - _Requirements: 4.5_
  - [x] 9.2 Create ConfigurationScreen for adding/editing configurations
    - Form for Xtream credentials (server URL, username, password)
    - File picker for local M3U files
    - Input for M3U network URL
    - Configuration name input
    - Save and cancel buttons
    - _Requirements: 2.1, 3.1, 3.2, 3.3, 3.4, 4.1_
  - [x] 9.3 Create ChannelListScreen
    - Display channels from selected configuration
    - Search and filter functionality
    - Category grouping
    - Refresh button for network configurations
    - Export to M3U button for Xtream configurations
    - Navigate to player on channel selection
    - _Requirements: 2.1, 2.4, 2.5, 3.6_
  - [x] 9.4 Create PlayerScreen with video player
    - Integrate Aliyun Player Widget
    - Display playback controls (play/pause, volume, fullscreen)
    - Show channel information
    - Handle playback errors with error overlay
    - Back button to return to channel list
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5_
  - [x] 9.5 Create FavoritesScreen
    - Display list of favorited channels
    - Allow playing channels from favorites
    - Allow removing favorites
    - _Requirements: 6.4, 6.5_
  - [x] 9.6 Create HistoryScreen
    - Display browse history in reverse chronological order
    - Allow playing channels from history
    - Clear history button
    - _Requirements: 7.3, 7.4, 7.5_
  - [x] 9.7 Implement navigation structure
    - Setup bottom navigation or drawer for main sections
    - Configure routes for all screens
    - Handle deep linking if needed

- [x] 10. Build reusable UI widgets
  - [x] 10.1 Create ConfigurationCard widget
    - Display configuration name and type
    - Show last refresh time
    - Action buttons (edit, delete, refresh)
    - _Requirements: 4.2, 4.3, 2.4, 3.6_
  - [x] 10.2 Create ChannelItem widget
    - Display channel name and logo
    - Show favorite indicator
    - Tap to play, long-press for options
    - _Requirements: 6.1, 6.2_
  - [x] 10.3 Create PlayerControls widget
    - Play/pause button
    - Volume slider
    - Fullscreen toggle
    - Progress bar (if applicable)
    - _Requirements: 5.5_

- [x] 11. Implement app branding and assets
  - [x] 11.1 Configure app icons from icon folder
    - Use flutter_launcher_icons package
    - Configure icons for all platforms using tv.png and tv.svg
    - Generate platform-specific icon assets
    - _Requirements: 8.1, 8.2_
  - [x] 11.2 Create splash screen with logo
    - Display logo during app initialization
    - Use flutter_native_splash package
    - _Requirements: 8.3_

- [x] 12. Add error handling and validation
  - [x] 12.1 Implement input validators
    - Validate configuration names (non-empty, length limits)
    - Validate URLs (proper format)
    - Validate Xtream credentials (all fields required)
    - _Requirements: 2.1, 3.3, 3.4, 4.1_
  - [x] 12.2 Add error handling for network operations
    - Handle timeout, connection errors
    - Display user-friendly error messages
    - Implement retry logic with exponential backoff
    - _Requirements: 2.3, 3.5, 5.3_
  - [x] 12.3 Add error handling for storage operations
    - Handle disk full errors
    - Handle corrupted data with fallback to defaults
    - _Requirements: 4.4, 6.3, 7.2_

- [x] 13. Platform-specific configurations
  - [x] 13.1 Configure Android platform
    - Add internet permission to AndroidManifest.xml
    - Add storage permissions if needed
    - Configure app name and icon
    - _Requirements: 1.1_
  - [x] 13.2 Configure iOS platform
    - Add network usage description to Info.plist
    - Add file access permissions
    - Configure app name and icon
    - _Requirements: 1.2_
  - [x] 13.3 Configure Web platform
    - Setup CORS handling if needed
    - Configure web-specific player settings
    - _Requirements: 1.3_
  - [x] 13.4 Configure Desktop platforms (Windows, macOS, Linux)
    - Configure file system access
    - Setup window management
    - Configure fullscreen support
    - _Requirements: 1.4, 1.5, 1.6_

- [x] 14. Performance optimizations
  - [x] 14.1 Implement channel list caching
    - Cache parsed M3U data to avoid re-parsing
    - Implement cache invalidation on refresh
  - [x] 14.2 Implement lazy loading for large channel lists
    - Use ListView.builder for efficient rendering
    - Implement virtual scrolling
  - [x] 14.3 Implement image caching for channel logos
    - Use cached_network_image package
    - Configure cache duration and size limits

- [x] 15. Final checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.

- [x] 16. Integration testing
  - [x] 16.1 Write integration test for configuration creation flow
    - Test complete flow from UI to storage
  - [x] 16.2 Write integration test for playback flow
    - Test channel selection to playback
  - [x] 16.3 Write integration test for favorites flow
    - Test adding/removing favorites across app restart

- [x] 17. Replace Aliyun Player Widget with Chewie
  - [x] 17.1 Update dependencies in pubspec.yaml
    - Remove aliplayer_widget dependency
    - Add video_player and chewie packages
    - Add wakelock_plus for screen wake lock during playback
    - Run flutter pub get
    - _Requirements: 9.1_
  - [x] 17.2 Refactor PlayerService to use Chewie
    - Replace FlutterAliplayer with VideoPlayerController and ChewieController
    - Update initialize() method to create video and chewie controllers
    - Update play() method to use VideoPlayerController.network()
    - Update pause(), resume(), stop() methods for video_player API
    - Update setVolume() method for video_player API
    - Update dispose() method to properly release both controllers
    - Update event listeners to use VideoPlayerController callbacks
    - _Requirements: 9.2, 9.4_
  - [ ]* 17.3 Write property test for Chewie player initialization
    - **Property 24: Chewie player initializes correctly**
    - **Validates: Requirements 9.2**
  - [ ]* 17.4 Write property test for Chewie error handling
    - **Property 25: Playback errors are handled appropriately**
    - **Validates: Requirements 9.3**
  - [x] 17.5 Update PlayerScreen to use Chewie widget
    - Replace placeholder player widget with Chewie widget
    - Use ChewieController from PlayerService
    - Remove custom controls overlay (Chewie provides built-in controls)
    - Update fullscreen handling to use Chewie's fullscreen
    - Ensure error overlay still works with Chewie
    - _Requirements: 9.2, 9.3_
  - [x] 17.6 Update PlayerViewModel for Chewie integration
    - Ensure PlayerViewModel works with updated PlayerService
    - Test state management with Chewie controllers
    - Verify history recording still works
    - _Requirements: 9.2_
  - [x] 17.7 Test video playback on all platforms
    - Test on Android, iOS, Web, Windows, macOS, Linux
    - Verify controls work correctly
    - Verify fullscreen mode works
    - Verify error handling works
    - _Requirements: 9.2, 9.3_

- [x] 18. Implement dark mode support
  - [x] 18.1 Create ThemeProvider
    - Create providers/theme_provider.dart
    - Implement ThemeProvider class extending ChangeNotifier
    - Define lightTheme and darkTheme using Material 3
    - Implement setThemeMode() method
    - Default to ThemeMode.system for automatic switching
    - _Requirements: 10.1, 10.2_
  - [x] 18.2 Integrate ThemeProvider in main.dart
    - Add ThemeProvider to MultiProvider
    - Configure MaterialApp with theme, darkTheme, and themeMode
    - Use ThemeProvider to control theme switching
    - _Requirements: 10.1, 10.2, 10.3_
  - [ ]* 18.3 Write property test for theme application
    - **Property 26: Dark mode theme applies correctly**
    - **Validates: Requirements 10.1, 10.2**
  - [ ]* 18.4 Write property test for theme updates
    - **Property 27: Theme changes update all UI components**
    - **Validates: Requirements 10.3**
  - [x] 18.5 Update all screens to use theme colors
    - Update HomeScreen to use Theme.of(context) colors
    - Update ConfigurationScreen to use theme colors
    - Update ChannelListScreen to use theme colors
    - Update FavoritesScreen to use theme colors
    - Update HistoryScreen to use theme colors
    - Ensure all text has proper contrast in both themes
    - _Requirements: 10.3, 10.5_
  - [x] 18.6 Update PlayerScreen for dark mode
    - Ensure player controls work in both themes
    - Update error overlay colors for both themes
    - Update channel info section colors
    - Maintain black background for video area in both themes
    - _Requirements: 10.4_
  - [ ]* 18.7 Write property test for player controls styling
    - **Property 28: Player controls maintain consistent styling**
    - **Validates: Requirements 10.4**
  - [x] 18.8 Update widgets for dark mode
    - Update ConfigurationCard colors
    - Update ChannelItem colors
    - Update PlayerControls colors (if still used)
    - Ensure icons are visible in both themes
    - _Requirements: 10.3, 10.5_
  - [ ]* 18.9 Write property test for contrast ratios
    - **Property 29: Text and icons have sufficient contrast**
    - **Validates: Requirements 10.5**
  - [x] 18.10 Test dark mode on all platforms
    - Test automatic theme switching based on system settings
    - Test on Android, iOS, Web, Windows, macOS, Linux
    - Verify all screens look good in both themes
    - Verify no readability issues
    - _Requirements: 10.1, 10.2, 10.3_

- [x] 19. Final checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
