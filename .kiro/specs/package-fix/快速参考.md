# 快速参考

## 🎯 问题

1. 图标没有正确显示
2. 打开程序报错：`Error: java/sql/DriverManager`

## ✅ 已修复

修改了 `composeApp/build.gradle.kts`，添加：
- `java.sql` 模块支持
- 正确的图标路径
- 完整运行时模块

## 🚀 立即重新打包

### macOS
```bash
./repackage.sh dmg
```

### Windows
```cmd
repackage.bat msi
```

### Linux
```bash
./repackage.sh deb
```

## 📍 打包文件位置

- macOS: `composeApp/build/compose/binaries/main/dmg/`
- Windows: `composeApp/build/compose/binaries/main/msi/` 或 `exe/`
- Linux: `composeApp/build/compose/binaries/main/deb/`

## ✔️ 验证

1. 安装新打包的应用
2. 启动应用（不应有 DriverManager 错误）
3. 检查图标是否正确显示
4. 测试添加播放列表功能

## 📚 详细文档

- `如何重新打包.md` - 详细打包步骤
- `测试检查清单.md` - 完整测试清单
- `修复总结.md` - 修复内容总结
- `桌面打包修复说明.md` - 中文详细说明
- `DESKTOP_PACKAGING_FIX.md` - 英文技术文档

## 🆘 需要帮助？

查看 `DESKTOP_PACKAGING_FIX.md` 的故障排除部分
